{
    "name": "meu-projeto",
    "version": "1.0.0",
    "scripts": {
        "dev": "npx concurrently --kill-others --names server,queue,vite --prefix-colors blue,magenta,red \"php artisan serve\" \"php artisan queue:listen --tries=1\" \"npm run dev\"",
        "build": "vite build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "vite": "^7.1.7"
    }
}
